
//IIFE (IIFY):Immediately Invocable Function Expression
var calcModule = (function () {
    function add(x, y) {
        console.log(x + y);
    }
    function multi(x, y) {
        console.log(x + y);
    }
    function sub(x, y) {
        console.log(x + y);
    }
});